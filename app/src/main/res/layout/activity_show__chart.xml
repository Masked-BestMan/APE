<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    tools:context="show.chart.Show_Chart_Activity"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Button
        android:layout_width="25dp"
        android:layout_height="25sp"
        android:id="@+id/backbutton"
        android:background="@drawable/back2"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="15dp"/>
    <TextView
        android:id="@+id/boom_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:gravity="center"
        android:text="@string/activity_boom_title_name"
        android:textSize="30sp"
        android:textColor="#ffffff"/>
    <android.support.design.widget.TabLayout
        android:id="@+id/boom_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/boom_label"
        app:tabTextAppearance="@style/TabLayoutTextStyle"
        app:tabGravity="fill"
        app:tabIndicatorColor="@color/tab_selected"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/tab_selected"
        app:tabTextColor="@color/tab_default"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/viewpager_boom"
        android:layout_below="@id/boom_tab_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="20dp"/>

</RelativeLayout>
